"""Interfaces: the ways a human or another program drives mamori.

The CLI lives here today. The OpenAI-compatible proxy planned for v0.2 is
another interface, not another application layer -- both drive the same
``PrivacySession``.
"""
