"""Frozen contract documents, shipped with the package.

A consumer validating against a schema it fetched over the network is
validating against whatever is there today. A consumer validating against a
copy it pinned is validating against a copy that drifts. These are the ones
this version emits.
"""
